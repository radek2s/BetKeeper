/**
 * Bet Status Enumeration
 * Represents the current state of an active bet
 */
export enum BetStatus {
  PENDING = "pending",
  RESOLVED = "resolved",
  COMPLETED = "completed",
  DELETED = "deleted",
}
